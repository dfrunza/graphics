#include <windows.h>
#include <tchar.h>
#include <stdio.h>

int WINAPI
WinMain(HINSTANCE h_instance, HINSTANCE h_prev_instance, LPSTR cmd_str, int show) {
  printf("Hello Windows!!\n");
  return 0;
}
